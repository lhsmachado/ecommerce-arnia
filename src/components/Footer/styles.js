import styled from "styled-components";
import { colors } from "../../theme/constants"

export const Container = styled.div`
    height: 86px;
    width: auto;
    border: 4px solid ${colors.roxo};
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 59px;
`